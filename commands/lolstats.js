exports.js  = async (client, message, args) => {
  const lol = require('lol-js');
  const lolClient = lol.client({
    apiKey: 'blahblahblah',
    cache: lol.redisCache({host: '127.0.0.1', port: 6379})
}




}